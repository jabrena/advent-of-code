package info.jab.aoc2015.day25;

/**
 * Represents a position (row, column) in a grid.
 * Immutable record following functional programming principles.
 */
public record Position(int row, int column) {}
